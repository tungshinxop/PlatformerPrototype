using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//This script in centipede boss
public class CentipedePatrol : MonoBehaviour
{
    public float centipedePatrolSpeed;//The speed at which the centipede will move left or right


    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }

   
}
